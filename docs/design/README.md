# design

Architecture: interfaces and components, gardened from `docs/wip/` sessions
into durable, as-built records.

Nothing lives here yet — the project's architecture currently lives in
`specs/DESIGN_1.md` (stack, pipeline, document format, producers, common
runtime, painters, target-hardware rules). It'll move here, or be superseded
by records written here, as future work gardens it in.

See the `sdd-working-memory-lifecycle` rule.
