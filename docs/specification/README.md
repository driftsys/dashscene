# specification

Requirements: what the system must do, gardened from `docs/wip/` sessions
into durable, as-built records.

Nothing lives here yet — the project's requirements currently live in
`specs/DESIGN_1.md` (goals G1-G3, hard requirements R1-R7). They'll move
here, or be superseded by records written here, as future work gardens
them in.

See the `sdd-working-memory-lifecycle` rule.
