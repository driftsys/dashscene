# What this bundle was taken on

    Pixel 5 (redfin), Android 14 / API 34, device

    taken     20240109T212654Z (the device's own clock)
    host      20260829T101500Z (this machine's clock)
    commit    0b02637
    branch    debt/v021-device-lane-phase2

Every timestamp in this bundle — the directory name, the logcat epochs
and every interval derived from them — is the device's. The host clock
above is provenance only: nothing here is timed by it, and the two
disagree whenever the device's clock is unset.

## getprop

    ro.product.model                 Pixel 5
    ro.product.device                redfin
    ro.product.cpu.abi               arm64-v8a
    ro.build.version.release         14
    ro.build.version.sdk             34
    ro.build.characteristics         nosdcard
    ro.hardware                      redfin
    ro.kernel.qemu                   
    ro.boot.qemu                     
