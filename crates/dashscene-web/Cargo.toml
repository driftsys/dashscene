[package]
name = "dashscene-web"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "wasm/tiny-skia painter — parked until pushed (docs/design/architecture.md)."

[lib]
name = "dashscene_web"

[dependencies]
