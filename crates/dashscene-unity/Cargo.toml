[package]
name = "dashscene-unity"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "The C representation of dashscene's painter boundary: an extern \"C\" surface holding dashpaint's value types FFI-representable, with layout pins and round-trip functions a non-Rust painter checks its own declarations against."
keywords = ["ffi", "abi", "layout", "rendering", "graphics"]
categories = ["development-tools::ffi", "graphics"]

[lib]
name = "dashscene_unity"

[dependencies]
dashpaint.workspace = true
