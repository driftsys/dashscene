[package]
name = "dashscene-core"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Semantic model: arena, node tree, layout tables, paint tables (DESIGN_1.md §5)."

[lib]
name = "dashscene_core"

[dependencies]
