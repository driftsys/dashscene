# Secret-scan values triaged as benign, one per line, matched as fixed strings.
#
# Read by `just secrets`' pattern grep. gitleaks reads `.gitleaksignore`
# instead, because its findings carry fingerprints and a fingerprint is more
# precise than a value. Both derive from the same triage; this file exists so
# a value is written down once rather than pasted into the justfile.
#
# Adding a line here is a claim that the value was read and understood. Do not
# add one to quiet a scan.
#
# --- Figma's own AWS access key IDs -----------------------------------------
#
# Captured inside S3 presigned thumbnail URLs when the tier-1 fixtures were
# pulled from the Figma REST API on 2026-07-12 (commits dc201bf, a6e6a6e).
# These are Figma's credentials, not this project's. Presigned URLs are
# time-limited and these expired in July 2026. They are absent from the
# current tree — verified by scanning a `git archive HEAD` export — and
# present only in history.
#
# Scrubbing them would rewrite 790 of 964 commits and invalidate the 48
# commit SHAs cited across the documentation, so they are accepted rather
# than removed. GitHub secret scanning will flag them on publication;
# dismiss as "used in tests".
AKIAQ4GOSFWC4KC5SY6U
AKIAQ4GOSFWCZHCZBGZX
