#:schema https://driftsys.github.io/git-std/schemas/v1/git-std.schema.json
# git-std configuration for dashscene-staging.
# See docs/decisions/house-style.md — dogfoods git-std's own conventions.

scheme = "semver"
# Explicit rather than "auto" (which only discovers crates/*): "auto"
# leaves no valid scope for repo-wide commits (docs, CI, hooks, deps).
# Keep in sync with crates/ when adding/removing a crate.
#
# A scope earns its place by making the changelog or a `git log --grep`
# more useful. One scope per top-level directory would dilute them, so
# the non-crate scopes are deliberate rather than exhaustive:
#
#   goldens     goldens/     the golden images and their diff tooling
#   corpus      corpus/      the stress corpus — captured Figma JSON,
#                            fonts, glyph atlases, photographic payloads,
#                            the DSL-generated stress cases, and the
#                            showcase scenes the demonstration draws
#                            (corpus/showcase/, a workspace member added
#                            by story #574). Mostly data; the capture tool
#                            is code but lives under importers/, so it
#                            takes that scope instead.
#   importers   importers/   the Deno/TypeScript Figma importer and its
#                            capture tool, which have their own
#                            toolchain and their own CI job
#   demo        demo/        the windowed showcase host — a workspace
#                            member with its own binary and its own CI
#                            job, not a published crate (story #571).
#                            Covers demo-web/ too, the browser host added
#                            by story #587: one scope for the hosts, as
#                            `corpus` covers corpus/showcase and `goldens`
#                            covers goldens/tooling
#
scopes = [
    "dashscene",
    "dashscene-core",
    "dashscene-engine",
    "dashscene-typeset",
    "dashscene-validator",
    "dashpaint",
    "dashscene-skia",
    "dashcue",
    "dashlang",
    "dashbuf",
    "dashc",
    "dashpack",
    "dashpack-astcenc-sys",
    "dashscene-unity",
    "dashscene-web",
    "dashscene-gpu",
    "goldens",
    "corpus",
    "importers",
    "demo",
    "repo",
    "docs",
    "ci",
    "hooks",
    "deps",
    "release",
]
strict = true
types = [
    "feat",
    "fix",
    "docs",
    "style",
    "refactor",
    "perf",
    "test",
    "chore",
    "ci",
    "build",
    "revert",
]

# One [[version_files]] entry per crate, pointing at its version string
# in Cargo.toml. Every version bump and changelog entry goes through
# `git std bump` (see `just release`), never a manual edit.
[[version_files]]
path = "crates/dashscene/Cargo.toml"
regex = 'version = "([^"]+)"'

[[version_files]]
path = "crates/dashscene-core/Cargo.toml"
regex = 'version = "([^"]+)"'

[[version_files]]
path = "crates/dashscene-engine/Cargo.toml"
regex = 'version = "([^"]+)"'

[[version_files]]
path = "crates/dashscene-typeset/Cargo.toml"
regex = 'version = "([^"]+)"'

[[version_files]]
path = "crates/dashscene-validator/Cargo.toml"
regex = 'version = "([^"]+)"'

[[version_files]]
path = "crates/dashpaint/Cargo.toml"
regex = 'version = "([^"]+)"'

[[version_files]]
path = "crates/dashscene-skia/Cargo.toml"
regex = 'version = "([^"]+)"'

[[version_files]]
path = "crates/dashcue/Cargo.toml"
regex = 'version = "([^"]+)"'

[[version_files]]
path = "crates/dashlang/Cargo.toml"
regex = 'version = "([^"]+)"'

[[version_files]]
path = "crates/dashbuf/Cargo.toml"
regex = 'version = "([^"]+)"'

[[version_files]]
path = "crates/dashc/Cargo.toml"
regex = 'version = "([^"]+)"'

[[version_files]]
path = "crates/dashscene-unity/Cargo.toml"
regex = 'version = "([^"]+)"'

[[version_files]]
path = "crates/dashscene-web/Cargo.toml"
regex = 'version = "([^"]+)"'

[[version_files]]
path = "crates/dashscene-gpu/Cargo.toml"
regex = 'version = "([^"]+)"'

[versioning]
tag_prefix = "v"
