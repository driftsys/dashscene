# Triaged gitleaks findings in git HISTORY, as `<rule>:<secret>` pairs.
#
# The history scan cannot use .gitleaksignore: a fingerprint is
# <file>:<rule>:<line>, and the same content appears at many different line
# numbers across history, so fingerprints do not converge. This file records
# the distinct finding set instead, and `just secrets` fails if the scan
# produces any pair not listed here. New history findings therefore fail the
# gate; known ones do not.
#
# Every entry below is either a Figma document identifier — a file key or a
# 40-hex component/style key, neither of which grants access on its own — or
# one of Figma's two expired presigned-URL AWS key IDs, which are recorded
# with their full reasoning in .secrets-triaged.
#
# Triaged 2026-08-10. Regenerate with:
#   gitleaks git --log-opts="--all" --config .gitleaks.toml \
#     --report-format json --report-path /tmp/h.json
#   jq -r '.[] | "\(.RuleID):\(.Secret)"' /tmp/h.json | sort -u
#
# Adding a line is a claim the value was read and understood.
aws-access-token:AKIAQ4GOSFWC4KC5SY6U
aws-access-token:AKIAQ4GOSFWCZHCZBGZX
generic-api-key:362c61b52efc2f937836171ff2f4937659e653ce
generic-api-key:39bab7c329f22eaac8daeaa25ad90a086eba28f7
generic-api-key:3d2de0afa1cf5080c561a25c257cf03b22098e5c
generic-api-key:43LWWEUuYJuK8iCUP9EXqU
generic-api-key:453a44eb5b72bb62a7f3c2b30476bd4c5e3f7ce2
generic-api-key:48AJL2nXxEYAPC423aM124
generic-api-key:8jlaJUSKF5qPWqnlHDe46R
generic-api-key:ARZJXh3QZKgtEWzA01eRmB
generic-api-key:BJ1sPobsTmNLQLWqtgjwJU
generic-api-key:CAWMOi1aDE7gTpNb6zKnxv
generic-api-key:Csc1LrucirZEngj6xX8M1d
generic-api-key:H4WG095xSFV6KzaA7JkHxf
generic-api-key:O7SBwtLtTsKg7FHqAYAnLg
generic-api-key:OAXcoWO5j5NghXV3ZKw9QV
generic-api-key:R1S4QqZtT54WVcYcFFuQoD
generic-api-key:VtbiQejcN6gYMmeWaaEy1b
generic-api-key:WQrfU66n2sZee3TOrgBZ33
generic-api-key:WgGz7q5ASAxQJpEnjkUhFm
generic-api-key:aVrn0XTElCIgiejyuCqMmW
generic-api-key:dqzSsQxPvj09i5VVG36JkG
generic-api-key:f0nG7azeYELWb9KZ2tLnu9
generic-api-key:lH9NgRr9lYRrIF8GizCNRU
generic-api-key:o2ku1XBJCxy07x9cLyYJa7
generic-api-key:qkMRe1bbE1CFVIsCDbijnb
generic-api-key:uJR3anK97E1ljyyjpThabG
generic-api-key:w8JdmcqdvZhD1qIB8GVK2u
#
# Three more of the same 40-hex component-key class, added 2026-08-14. They
# arrived when the prototype captures were re-taken (commit 6aac57f): a
# re-capture mints new component keys, and this file matches by value, so
# new keys are new findings by construction rather than by fault.
#
# Read and understood: all three are the `key` of an entry in
# corpus/figma-fixtures/prototype-refused.json's `components` and
# `componentSets` maps. A component key names a component inside Figma's
# component system and is not a credential; resolving one through the REST
# library endpoints needs an authenticated token with access to the library
# the component was published to.
#
# It also exposes nothing this project has not already published on purpose.
# `prototype-refused` is one of the 32 fixtures in
# corpus/figma-fixtures/manifest.json, so its FILE key — the consequential
# one, being unrotatable and enough to open a link-shared file — is already
# public, and docs/decisions/figma-file-keys-are-published.md rules every
# fixture explicitly link-viewable after a full read-through. The design
# these components live in is open to anyone with the link by decision.
#
# Note what `remote: false` does NOT say. An earlier revision of this note
# read it as "never published to a library". It means the component is
# defined in this file rather than imported from an external one; a local
# component can be published to a team library and still read false here.
# The two paragraphs above do not rest on it.
generic-api-key:00cb1fd673814806eed23d684df7894ff6ca7b11
generic-api-key:4a2ac866898c63cd052cf52e7d9a6bc47924aed7
generic-api-key:e41924557ca35436e1ab8fe393a9f0e8927fa8c0

# The three keys the same file carried BEFORE that re-capture: `tone=cool`,
# `tone=warm` and their set again, minted by the earlier capture. Same file,
# same class, different capture.
#
# An earlier revision of this note attributed them to
# `story/v019-text-across-the-c-abi` and said both sets stayed reachable "until
# that branch lands or is discarded". That was wrong, and acting on it would
# have deleted entries that can never be deleted: these three are the values
# `main`'s own copy of the fixture carried, so they are in `main`'s history
# permanently and no branch deletion reaches them. Both sets are reachable for
# good.
generic-api-key:05d60e708978a1c4c192dfe958189a205c564388
generic-api-key:3c5cb3d094521a1669e688d5705792c36541e9fa
generic-api-key:fa79b0a5b0bf3ec41fa4a97e1185c334b02ff9ee
#
# The three component keys of the OTHER prototype capture,
# corpus/figma-fixtures/prototype-smart-animate.json, which the same re-capture
# reminted. They name `state=rest`, `state=active` and their set,
# `prototype-smart-animate`.
#
# They are written here because .gitleaksignore could not record them. A
# fingerprint pins <file>:<rule>:<line> and carries no value, and the
# re-capture replaced all three keys at the exact lines the existing
# fingerprints already pinned — 4178, 4186 and 4196. So the tree gate stayed
# silent across a change of content, and nothing forced the re-triage the
# fingerprint scheme exists to force. Measured: with .gitleaksignore removed
# the tree scan reports all three at those lines; with it in place it reports
# nothing. Reading them is that missing re-triage.
#
# Same class and same reasoning as the block above: a component `key` is a
# 40-hex content address inside Figma's component system, not a credential, and
# resolving one through the REST library endpoints needs an authenticated token
# with access to the library it was published to. `prototype-smart-animate` is
# one of the 32 fixtures in corpus/figma-fixtures/manifest.json, so its
# consequential identifier — the FILE key, unrotatable and enough to open a
# link-shared file — is already published on purpose by
# docs/decisions/figma-file-keys-are-published.md.
generic-api-key:61b0a39220924f6f5cffd18dbab1e867737791ac
generic-api-key:defaf7851fd37c9ceeec122a4f425236910c6a99
generic-api-key:dff7422f1460cd74e6dfbd80fb5a35eb0eba7672
